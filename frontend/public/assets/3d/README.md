# 3D Assets

Place your GLB models here. The `FluidGlass` component expects:

- `lens.glb`
- `bar.glb`
- `cube.glb`

If you use different filenames or locations, update the paths in `FluidGlass.tsx` accordingly.***
