# Demo Images

The optional `FluidGlass` demo uses a few background images. Add your own files here and update the paths in `FluidGlass.tsx` if needed:

- `cs1.webp`
- `cs2.webp`
- `cs3.webp`

Missing files will just log 404s in the console.***
